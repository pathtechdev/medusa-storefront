[build]
builder = "NIXPACKS"

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmd = "npm ci"

[phases.build]
cmd = "npm run build"

[start]
cmd = "node .next/standalone/server.js"

[variables]
NEXT_PUBLIC_MEDUSA_BACKEND_URL = "https://yingfloral-production.up.railway.app"
